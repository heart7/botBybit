# Dependencies
node_modules/

# Secrets — never commit real env files (service keys stay server-side only)
.env
.env.*
!.env.example

# Expo / React Native
.expo/
dist/
web-build/
*.tsbuildinfo

# Supabase local stack
supabase/.branches/
supabase/.temp/

# OS
.DS_Store
